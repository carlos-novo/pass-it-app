/**
 * CAPA DE DOMINIO — Frontend
 *
 * Contiene los tipos y lógica de presentación de dominio.
 * Re-exporta los tipos compartidos del paquete @pass-it/tipos-compartidos
 * para que el resto de la app solo necesite importar desde @dominio.
 */
export * from '@tipos-compartidos';
